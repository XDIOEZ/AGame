using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Boss_Ghost : Entity
{
    // Start is called before the first frame update
    void Start()
    {
        base.Start_();
    }

    // Update is called once per frame
    void Update()
    {
        base.Update_();
    }
}
